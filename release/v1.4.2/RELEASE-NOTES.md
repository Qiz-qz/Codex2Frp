# Codex2Frp v1.4.2

This backend-only public snapshot corresponds to the internal Codex2Frp 2.4.2 capability line.

- Adapts schema negotiation to installed Codex CLI 0.144.5 without weakening exact hash and critical-union validation.
- Adds confirmed renderer-backed desktop controls and native task navigation for supported phone actions.
- Preserves desktop-visible transient status, operation adjacency, image-view activity, and safe subagent lifecycle rows.
- Restores historical image previews when verified image magic disagrees with a misleading legacy suffix.
- Keeps background synchronization focus-free and restores window, focus, and minimized state after explicit phone UI actions.

Only the Windows backend installer and its SHA-256 checksum are published as GitHub Release assets. Mobile client packages, signing materials, screenshots, and local state are not included.
